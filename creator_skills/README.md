# Creator Skills

这个目录用于维护你自己的内容生成规则。后端会在 Gemini / MiniMax 生成文案前读取本目录下所有 `.md` 文件，并把它们作为长期规则注入 System Prompt。

使用方式：

1. 新增一个 Markdown 文件，例如 `05_my_voice.md`。
2. 写清楚适用场景、禁止项、表达风格和示例。
3. 提交并部署后，后续文案生成会自动读取。

建议每个文件只写一种规则，避免混在一起难以维护。
